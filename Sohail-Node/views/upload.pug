doctype html
head
  title File Upload

h1 File Upload using Multer
form(action='upload_photo' method='post' enctype='multipart/form-data')
  input(type='file' name='photo')
  input(type='submit' value="Upload Photo")
